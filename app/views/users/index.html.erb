<h2>ユーザー</h2>
  <% @users.each do |user| %>
    <%= user.name %>
    <%= user.email %>
    <%= render 'follow_form', user: user  %>
    <%= link_to 'ユーザー情報', user_path(user), class: 'btn btn-primary' %>
    <br>
  <% end %>
